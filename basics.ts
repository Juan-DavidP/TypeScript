//JavaScript => con esteroides
//JavaScript = TypeScript

function suma(a: number, b: number): number {
    return a + b;
}

//typescript -> mientras se desarrolla

//bundler
//transpilacion -> transformacion del lenguaje de alto nivel a alto nivel EJ:(de ts a js)
//uglifying
//minificacion
//tree shaking

let a:number[] = [1, 2, 3,4];

let b:[number,string,boolean] = [1, "J", true];

let c = Array<number>;

console.log(c);

